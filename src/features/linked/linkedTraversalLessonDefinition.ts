import type { PredictionCheckpointDefinition } from '../learning/PredictionCheckpoint'

export const linkedTraversalLesson = {
  slug: 'linked-traversal', title: 'Traversal & Search', lessonLabel: 'LESSON 03.03', duration: '10 MIN', tagline: 'Start at head. Inspect one node. Follow one link. Stop only when the target is found or next becomes null.', timeComplexity: 'O(n)', spaceComplexity: 'O(1)',
  problem: { title: 'Find a stop from only the route head', scenario: 'A dispatch diagnostic receives the head of a linked delivery route and a stop code. It must determine whether that stop is still scheduled.', constraints: ['No array index or lookup table is available.', 'A missing stop is a valid query.', 'Only constant extra workspace is allowed.'], question: 'How can the service prove a stop exists—or prove it is absent—when each node reveals only its successor?' },
  prediction: { question: 'Why can linked-list search require inspecting every node?', options: [{ id: 'only-next', label: 'Each node reveals only its next reference, so later nodes cannot be skipped safely.' }, { id: 'values-move', label: 'Node values move after every comparison.' }, { id: 'head-changes', label: 'Head changes during every traversal.' }, { id: 'memory-copy', label: 'The entire list must first be copied.' }], correctOptionId: 'only-next', hint: 'What information do you have before following the current node’s next reference?', explanation: 'A singly linked list exposes no random-access path to a later position. To reach or rule out a later node, traversal must follow every preceding next reference.' } satisfies PredictionCheckpointDefinition,
} as const
