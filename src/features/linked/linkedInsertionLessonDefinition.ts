import type { PredictionCheckpointDefinition } from '../learning/PredictionCheckpoint'

export const linkedInsertionLesson = {
  slug: 'linked-insertion', title: 'Linked List Insertion', lessonLabel: 'LESSON 03.01', duration: '11 MIN', tagline: 'Allocate one node. Preserve the successor. Redirect the predecessor. Move no existing values.', timeComplexity: 'O(1)', spaceComplexity: 'O(1)',
  problem: { title: 'Insert a stop into a live delivery route', scenario: 'A dispatch service stores a driver’s ordered stops as linked records. A priority pickup must be inserted after a known stop while other systems retain references to existing stop records.', constraints: ['Existing node identities must remain stable.', 'The predecessor node is already known.', 'The rest of the route must never become unreachable.'], question: 'In which order should links change so the new stop becomes reachable without losing the remaining route?' },
  prediction: { question: 'Why should new.next be assigned before predecessor.next during insertion?', options: [{ id: 'preserve', label: 'It preserves a reference to the original successor.' }, { id: 'sort', label: 'It sorts the list automatically.' }, { id: 'allocate', label: 'It allocates the predecessor node.' }, { id: 'index', label: 'It updates every later array index.' }], correctOptionId: 'preserve', hint: 'What is the only link to the remainder of the list before insertion?', explanation: 'The predecessor initially holds the route to the successor. Copying that link into new.next first preserves the remainder before predecessor.next is redirected.' } satisfies PredictionCheckpointDefinition,
} as const
