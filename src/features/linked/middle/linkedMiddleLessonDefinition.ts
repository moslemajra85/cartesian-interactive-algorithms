import type { PredictionCheckpointDefinition } from '../../learning/PredictionCheckpoint'

export const linkedMiddleLesson = {
  slug: 'linked-find-middle', title: 'Find the Middle', lessonLabel: 'LESSON 03.07', duration: '10 MIN', tagline: 'Move slow once and fast twice. When fast reaches the boundary, slow has covered half the path.', timeComplexity: 'O(n)', spaceComplexity: 'O(1)',
  problem: { title: 'Split a streaming playlist without counting first', scenario: 'A media service receives only the head of a linked playlist and must split it near the middle for two processing workers. The list does not store its length.', constraints: ['Only one forward pass is allowed.', 'Nodes cannot be copied into an array.', 'For an even list, choose the first node of the second half.'], question: 'How can the service locate the split point without first traversing once to count every node?' },
  prediction: { question: 'Why is slow at the middle when fast reaches the list boundary?', options: [{ id: 'ratio', label: 'Fast travels twice as many links, so slow covers half the total path.' }, { id: 'values', label: 'Slow compares and sorts every node value.' }, { id: 'backward', label: 'Fast walks backward from the tail.' }, { id: 'length', label: 'Each node stores the complete list length.' }], correctOptionId: 'ratio', hint: 'Compare the distance traveled by the two pointers in every round.', explanation: 'Every round advances slow by one link and fast by two. When fast consumes the available path, slow has consumed half of it and therefore marks the midpoint.' } satisfies PredictionCheckpointDefinition,
} as const
